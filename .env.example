# Copy to .env.local and fill in real values. Never commit .env.local.
# Server-only secrets: do NOT prefix with NEXT_PUBLIC_, which would inline
# them into the client bundle.

OPENAI_API_KEY=
REPLICATE_API_TOKEN=
